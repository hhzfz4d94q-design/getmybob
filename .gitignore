# Local secrets — NEVER commit
.env

# Personal data — stays local on Amit's Mac, never goes to GitHub
resume.json

# Personal application drafts
applications/

# Local cache / OS junk
__pycache__/
*.pyc
.DS_Store

# Local-only SQLite journal
jobs.db-journal
jobs.db-wal
jobs.db-shm

# Sample preview (regenerated each run)
sample_dashboard.html

# JSDOM was installed locally for parse-checking the dashboard JS; never commit it.
node_modules/

# Local scratch files parked here from the Cowork working folder — not source code, stay local
EMAIL_DIGEST_SETUP.md
fetch_jobs_PATCHED.py
getmemyjob-helper-README.md
getmemyjob-helper.zip
getmemyjob-helper-firefox.zip

# Playwright button-test failure screenshot (CI artifact, not committed)
tests/failure-screenshot.png
